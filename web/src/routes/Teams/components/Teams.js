import React from 'react';
import PropTypes from 'prop-types';

export const Teams = ({ fetchPlayers }) => (
    <div>
        <h2>Teams screen</h2>
    </div>
);

Teams.propTypes = {

};

export default Teams;
