import React from 'react';
import PropTypes from 'prop-types';

export const Players = ({ fetchPlayers }) => (
    <div>
        <h2>Players screen</h2>
    </div>
);

Players.propTypes = {

};

export default Players;
