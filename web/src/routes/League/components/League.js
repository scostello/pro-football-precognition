import React from 'react';
import PropTypes from 'prop-types';

export const League = ({ fetchPlayers }) => (
    <div>
        <h2>League screen</h2>
    </div>
);

League.propTypes = {

};

export default League;
