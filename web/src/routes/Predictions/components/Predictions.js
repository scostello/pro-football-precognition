import React from 'react';
import PropTypes from 'prop-types';

export const Predictions = ({ fetchPlayers }) => (
    <div>
        <h2>Predictions screen</h2>
    </div>
);

Predictions.propTypes = {

};

export default Predictions;
